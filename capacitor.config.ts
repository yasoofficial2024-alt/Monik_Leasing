import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.monikgroup.lease',
  appName: 'Leasing Calculator',
  webDir: 'dist'
};

export default config;
